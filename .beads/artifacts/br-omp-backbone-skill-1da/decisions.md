# Decisions: br-omp-backbone-skill-1da

## Decision Log

| # | Decision | Rationale | Confidence |
|---|----------|-----------|------------|
| 1 | Replace entire conventions.md Workflow section rather than making individual line edits | The current 7-step workflow has 5 bugs (wrong output, missing phases, wrong names, wrong merge semantics). Fixing each individually risks creating an inconsistent intermediate state. A full section replacement is atomic and verifiable. | High |
| 2 | Add /brainstorm as workflow step 1, not step 0 | Workflow conventions across the codebase use 1-based numbering (AGENTS.md command table, RULES.md). Step 0 would be unprecedented and confusing. /brainstorm is a first-class command with its own 182-line file — it deserves step 1. | High |
| 3 | Add /plan as workflow step 3 with explicit artifact list | /plan produces 3 artifacts (plan.md, tasks.md, context-capsule.md). Listing them in the workflow prevents agents from thinking /create already produced them. This is the root cause bug — agents skip /plan because conventions.md told them /create already handled it. | High |
| 4 | Rename "Implement" to "Ship" in workflow | The command is `/ship`, not `/implement`. Using "Ship" in the workflow keeps naming consistent with the command file and AGENTS.md command reference table. The convention is to match command names (e.g., "Create" → `/create`, "Plan" → `/plan`). | High |
| 5 | Rename "Triage" to "Brainstorm" in workflow step 1 | "Triage" was the old name. The command was rewritten as `/brainstorm` in commit 0ba3013 with an 8-phase exploration protocol. "Triage" is a bv robot command (`--robot-triage`), not a workflow phase. The actual entry point is `/brainstorm`. | High |
| 6 | Change "after merge" to "after review approval" in Close step | RULES.md line 27 states: "The human always gets the last call on merges — the agent proposes, the human decides." The Close step should reflect this: close happens after review approval, merge happens at human discretion. The old text "after merge" incorrectly implies the agent merges. | High |
| 7 | Fix AGENTS.md prefix from "omp" to "br-omp" | All 10 actual bead IDs use the prefix `br-omp` (verified via `br list --all --json`). The `br-` part comes from the beads_rust tool prefix requirement; `omp` is the repo-specific suffix. AGENTS.md had only `omp` which, while not causing runtime errors (br resolves IDs by suffix), misleads agents about how beads are named. | High |
| 8 | Restructure AGENTS.md tree diagram to separate .beads/ and .omp/ as top-level directories | The current tree mixes .beads/ artifact files with .omp/ configuration at confusing nesting levels. A clean separation makes the project structure immediately clear to new developers and agents. Design system CSS files (tokens.css, base.css, primitives.css) are removed from the tree since they don't exist at those paths in this repo. | Medium |
| 9 | Update "Keep each under 1KB" to "Tier 1 target: under 2KB. Tier 2 target: under 3KB." | The 1KB target is aspirational and only project.md (1,259 bytes) comes close. The tier system (Tier 1 = always in context, Tier 2 = on-demand) is already defined in AGENTS.md lines 95-110. Aligning the size targets with tiers gives agents actionable guidance: keep the context-inlined files small, allow the on-demand files to be more detailed. | Medium |
| 10 | Update project.md Current Phase after audit completion | project.md is the "what's happening now" file. After this bead closes, the milestone should reflect the completed audit. The "Next" field guides the next bead — suggesting workflow-gate enforcement verification. | Medium |
| 11 | Make Review step description specific: "5 parallel agents, confidence filter ≥80" | The current text "runs parallel agents, confidence filter" is vague. The actual /review command (review.md) launches 5 agents (spec compliance, plan compliance, bug scan, git history, code comment compliance) and filters at confidence ≥80. Being specific helps agents understand the review process without reading the command file. | Medium |

## Rejected Alternatives

| # | Alternative | Why Rejected | Risk if Re-introduced |
|---|-------------|--------------|----------------------|
| 1 | Fix only the `/create` output bug (line 50) and leave everything else | Would miss 11 other audit findings. Would create a workflow with Brainstorm missing but Create correct — still confusing. Would leave stale "Triage" naming and "after merge" semantics. | Low — even a partial fix is better than no fix, but the other bugs would surface again |
| 2 | Rewrite conventions.md Workflow section to reference AGENTS.md table instead of duplicating | AGENTS.md and conventions.md serve different audiences. AGENTS.md is the canonical project manifesto loaded by agents. Conventions.md is the inlined Tier 1 memory file. Both should be self-contained — an agent reading only conventions.md should get correct workflow info. Cross-referencing creates a dependency where conventions.md is incomplete without AGENTS.md. | Medium — agents might miss conventions if they only load one file |
| 3 | Remove the Workflow section from conventions.md entirely and rely on AGENTS.md | Conventions.md is specifically about "how we build" — removing the workflow would gut its purpose. The section is only 7 lines (growing to 8) — keeping it is low cost. AGENTS.md may not always be loaded (e.g., in contexts where only memory files are injected). | High — agents without AGENTS.md context would have no workflow guidance |
| 4 | Add automated CI check for conventions.md ↔ AGENTS.md consistency | Would require a parser for both files and a comparison engine. This is a separate, larger piece of work (estimated 90+ minutes). While valuable, it's out of scope for this bead. The manual audit done here is sufficient for now. | Low — documentation drift can happen again, but the fixed files are now aligned |
| 5 | Change the br prefix to match AGENTS.md (i.e., change prefix from "br-omp" to "omp") | The prefix is set by br during initialization and encoded in the issues.jsonl database. Changing it would require database migration across 10 beads. The correct fix is to update the documentation to match reality, not vice versa. | High — database corruption risk, bead ID invalidation |
| 6 | Fix tree diagram by removing it entirely | The tree diagram provides a quick visual map of the project structure. While it's documentation-only, removing it would make the project less approachable to new developers. Fixing the structure is low effort and preserves the value. | Low — tree diagram is nice-to-have, not critical |

## Assumptions

| # | Assumption | Validation | Invalidation Impact |
|---|------------|------------|---------------------|
| 1 | The command files (create.md, plan.md, etc.) are the ground truth for artifact outputs | Verified by reading all 10 command files and cross-referencing their "Writes" sections against AGENTS.md table (lines 20-31). All commands match AGENTS.md. | If a command file is wrong, both AGENTS.md and conventions.md would need updating. But command files are the implementation — they define what actually happens. |
| 2 | No other file references the old workflow (e.g., "PRD + plan + tasks") | Searched `.omp/` with `grep -r "PRD + plan" .omp/` — only conventions.md matched. Searched for "Triage" as workflow step — only conventions.md. | If another file references the old workflow, it would need separate fixing. This is unlikely given the search results. |
| 3 | The br prefix "br-omp" won't change unless the repo is re-initialized | br prefix is set at `br init` time and stored in the issues.jsonl database. Changing it requires a manual database edit or re-initialization, both of which are rare and intentional. | If prefix changes, AGENTS.md and conventions.md would need updating again. This is a very low-probability event. |
| 4 | Design system CSS files don't exist at the paths shown in the old tree diagram | Verified: `ls .omp/memory/project/tokens.css .omp/memory/project/base.css .omp/memory/project/primitives.css` returns "No such file". These files don't exist in this repo. | If they're added later, the tree diagram would be incomplete. But the tree is a snapshot, not a live directory listing. |
| 5 | The skill count is 18, not 17 as shown in the old tree | Verified: `ls .omp/skills/ | wc -l` returns 18. The honcho-memory skill was added in br-omp-backbone-skill-iej and the tree wasn't updated. | If more skills are added, the tree count becomes stale again. The tree is a snapshot. |
